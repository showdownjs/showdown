
Define a function in javascript:

```
function MyFunc(a) {
    var s = '`';
}
```

And some HTML

```html
<div>HTML!</div>
```

Use more than 3 backticks

`````
some code
`````

Use tilde as delimiter

~~~
another piece of code
~~~
